---
layout: page
title: Writing
metaDesc: I don't get the chance to write often, but I do ocassionally write some words about the web!
---

<p>Although I don't get the chance to write often, I do so ocassionally and post it here!</p>

{% set postList = collections.posts %}
{% if collections.posts.length %}
  <ol reversed class="[ latest-posts ][ flow flow-space-700 ]">
    {% for item in postList | reverse %}
      {% include 'partials/postItem.njk' %}
    {% endfor %}
  </ol>
{% endif %}

<nav class="[ text-400 ][ flow-space-major ]">
  All Tags:
  {% for tag, items in collections %}
    {% if items.length and tag not in [
      'all',
      'posts',
      'work',
      'featuredWork'
    ] %}
      <a href="/writing/tag/{{ tag | slug }}/">{{ tag }}</a>
    {% endif %}
  {% endfor %}
</nav>
